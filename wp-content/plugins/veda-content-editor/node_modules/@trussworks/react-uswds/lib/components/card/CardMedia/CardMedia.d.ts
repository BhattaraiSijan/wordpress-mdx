import { default as React, JSX } from 'react';
export type CardMediaProps = {
    exdent?: boolean;
    inset?: boolean;
    imageClass?: string;
    children: React.ReactNode;
} & JSX.IntrinsicElements['div'];
export declare const CardMedia: ({ exdent, inset, imageClass, children, className, ...mediaProps }: CardMediaProps) => JSX.Element;
export default CardMedia;
