import { default as React, JSX } from 'react';
export type ExtendedNavProps = {
    primaryItems: React.ReactNode[];
    secondaryItems: React.ReactNode[];
    onToggleMobileNav?: (event: React.MouseEvent<HTMLButtonElement, MouseEvent>) => void;
    mobileExpanded?: boolean;
} & JSX.IntrinsicElements['nav'];
export declare const ExtendedNav: ({ primaryItems, secondaryItems, mobileExpanded, children, className, onToggleMobileNav, ...navProps }: ExtendedNavProps) => JSX.Element;
export default ExtendedNav;
