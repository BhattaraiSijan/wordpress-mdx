import { default as React, JSX } from 'react';
export type SideNavProps = {
    items: React.ReactNode[];
    isSubnav?: boolean;
};
export declare const SideNav: ({ items, isSubnav, ...ulProps }: SideNavProps) => JSX.Element;
