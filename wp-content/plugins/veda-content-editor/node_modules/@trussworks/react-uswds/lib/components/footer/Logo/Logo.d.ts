import { default as React, JSX } from 'react';
export type LogoProps = {
    size?: 'big' | 'medium' | 'slim';
    heading?: React.ReactNode;
    image: React.ReactNode;
    className?: string;
};
export declare const Logo: ({ size, heading, image, className, }: LogoProps) => JSX.Element;
