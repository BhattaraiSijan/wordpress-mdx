import { default as React, JSX } from 'react';
import { ValidationStatus } from '../../../types/validationStatus';
import { LegacyInputRef } from '../../../types/legacyInputRef';
export type SelectProps = {
    id: string;
    name: string;
    className?: string;
    children: React.ReactNode;
    validationStatus?: ValidationStatus;
    inputRef?: LegacyInputRef<HTMLSelectElement>;
} & JSX.IntrinsicElements['select'];
export declare const Select: ({ id, name, className, inputRef, children, validationStatus, ...inputProps }: SelectProps) => JSX.Element;
