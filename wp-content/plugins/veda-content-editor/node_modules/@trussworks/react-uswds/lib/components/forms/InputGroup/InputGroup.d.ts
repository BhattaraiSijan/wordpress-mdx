import { default as React, JSX } from 'react';
export interface InputGroupProps {
    children: React.ReactNode;
    className?: string;
    error?: boolean;
}
export declare const InputGroup: ({ children, className, error, }: InputGroupProps) => JSX.Element;
