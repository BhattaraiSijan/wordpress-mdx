import { default as React, JSX } from 'react';
export type LabelProps = {
    children: React.ReactNode;
    htmlFor: string;
    className?: string;
    error?: boolean;
    hint?: React.ReactNode;
    srOnly?: boolean;
    requiredMarker?: boolean;
} & JSX.IntrinsicElements['label'];
export declare const Label: ({ children, htmlFor, className, error, hint, srOnly, requiredMarker, ...labelProps }: LabelProps) => JSX.Element;
export default Label;
