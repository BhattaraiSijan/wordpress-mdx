import { default as React, JSX } from 'react';
export type PrimaryNavProps = {
    items: React.ReactNode[];
    onToggleMobileNav?: (event: React.MouseEvent<HTMLButtonElement, MouseEvent>) => void;
    mobileExpanded?: boolean;
} & JSX.IntrinsicElements['nav'];
export declare const PrimaryNav: ({ items, onToggleMobileNav, mobileExpanded, children, className, ...navProps }: PrimaryNavProps) => JSX.Element;
export default PrimaryNav;
