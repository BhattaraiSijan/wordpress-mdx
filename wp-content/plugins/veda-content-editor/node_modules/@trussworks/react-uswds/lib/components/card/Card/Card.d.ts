import { default as React, JSX } from 'react';
import { GridLayoutProp } from '../../grid/Grid/Grid';
export type CardProps = {
    layout?: 'standardDefault' | 'flagDefault' | 'flagMediaRight';
    headerFirst?: boolean;
    containerProps?: React.HTMLAttributes<HTMLDivElement>;
} & JSX.IntrinsicElements['li'] & GridLayoutProp;
export declare const Card: ({ layout, headerFirst, children, className, gridLayout, containerProps, ...liProps }: CardProps) => JSX.Element;
export default Card;
