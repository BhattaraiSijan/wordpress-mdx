import { default as React, JSX } from 'react';
export type FormGroupProps = {
    children: React.ReactNode;
    className?: string;
    error?: boolean;
};
export declare const FormGroup: ({ children, className, error, }: FormGroupProps) => JSX.Element;
